# Installation des machines Ansible



## builder et configurer l'environnement avec docker


```
cd existing_repo
```
utiliser docker compose pour monter un serveur ansible et deux nodes cibles
```
docker compose up -d --build
